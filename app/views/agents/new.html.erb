<h1>New agent</h1>

<% form_for(:agent, :url => {:controller => :agents, :action => :create},
                            :html => { :multipart => true }) do |f| -%>
  <%= render(:partial => 'form', :object => [f, 'newagent_game'])%>
  <%= submit_tag "Create" %>
<% end %>
<br/>
<%= link_to 'Back', :action => 'index' %>

